--scene generated with template

local {{Scene}} = {}

function {{Scene}}:enter()
  --this will be executed when the scene change to this
end

function {{Scene}}:leave()
  --this will be executed before the scene change to a new scene
end

function {{Scene}}:update(dt)
  --update code
end

function {{Scene}}:draw()
  --draw code
end

-- you can add mousepressed, mousereleased, keypressed and keyreleassed funcionts

return {{Scene}}